<h2>Users index view</h2>

    <container>
        <table class="table table-hover">
            <% @users.each do |a| %>
              <tr>
                <td><%= a.name %></td>
                <td><%= a.email %></td><br>
              </tr>
            <% end %>
        </table>
    </container>

